<%= form_for(@employee) do |f| %>
  <% if @employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@employee.errors.count, "error") %> prohibited this employee from being saved:</h2>

      <ul>
      <% @employee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :full_name %><br>
    <%= f.text_field :full_name %>
  </div>


  <div class="field">
      <%= f.label "Shop" %><br />
      <%= f.select(:shop_id, @shops.collect { |s| [s.name, s.id] }, { include_blank: true } ) %>
  </div>
  
  <div class="field">
      <%= f.label "Department" %><br />
      <%= f.select(:department_id, @departments.collect { |d| [d.role, d.id] }, { include_blank: true } ) %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
